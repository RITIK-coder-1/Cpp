#include <iostream>

using namespace std; // we can directly include namespace here.

int main(){
    cout << "Hello Ritik!" << endl; // The entities that are being provided by the standard library can be used directly

    return 0;
}